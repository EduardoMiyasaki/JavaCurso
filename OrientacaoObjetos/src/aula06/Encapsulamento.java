package aula06;

public class Encapsulamento {

    public static void main(String[] args) {
    /*
     Abstração é pegar apenas coisas que importam da classe
     Exemplo você tem uma loja e vai pegar dados dos cliente
     você vai na classe pessoa e pega apenas algumas informações como cpf,endereco,nome
     e não tudo como cor,altura,etc.*/

    /* Encapsulamento serve para dar proteceção e também dar um padrão
     * um software encapsulado protege o codigo do progamador e o progamador do codigo
     *
     * "Ocultar partes independentes da implementação,
     * permitindo construir partes invisíveis ao mundo exterior"
     *
     * ou seja você não precisa saber como funciona, mas ele vai funcionar
     *
     * um objeto encapsulado tem que ter interface
     *
     * interface = "Lista de serviços fornecidos por uma componente,é o contato com o mundo exterior,que define o que pode ser feito com um objeto dessa clase"
     *
     * Vantagens de encapsular = tornar mudanças invisiveis, exemplo:troca de pilha
     *
     * facilitar reutilização de código
     *
     * Reduzir efeitos colaterais
     */
        ControleRemoto controle1 = new ControleRemoto();
    }
}
